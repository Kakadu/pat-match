(env
  (dev
    (flags (-strict-sequence -strict-formats -short-paths -keep-locs -w -3-9-32))))

(executable
  (name main)
  (modules main)
  (flags (-rectypes))
  (libraries ocanren)
  (preprocess (pps OCanren-ppx.ppx_repr GT.ppx))
)


(rule
 (targets work.ml)
 (deps    work.ml2mk.ml)
 (action  (run noCanren work.ml2mk.ml -o %{targets})))

(executable
  (name main2)
  (modules main2 work)
  (flags (-rectypes))
  (libraries ocanren ocanren.tester)
  (preprocess (pps OCanren-ppx.ppx_repr GT.ppx))
)

(executable
  (name main_gadt)
  (modules main_gadt)
  (flags (-rectypes))
  (libraries ocanren ocanren.tester)
  (preprocess (pps OCanren-ppx.ppx_repr OCanren-ppx.ppx_fresh  GT.ppx))
)
